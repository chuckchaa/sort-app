import styles from './Array.module.scss'
import Bar from '../Bar'

const Array = () => {
  return (
    <div className={styles.root}>
      <Bar width={10} height={20} color={'#FF7480'} />
    </div>
  )
}

export default Array
